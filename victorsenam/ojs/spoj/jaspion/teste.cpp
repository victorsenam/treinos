#include <bits/stdc++.h>

using namespace std;

map<string, string> dict;
string from, to;
char auxil[82];
int t, m, n;

int main () {
    while (scanf(" %[a-z ]", auxil) != EOF) {
        printf("%s\n", auxil);
    }
}
