#include<bits/stdc++.h>
using namespace std;

int main()
{
    for( int aux = 0; aux < INT_MAX; aux += 8)
        if( (aux%1000)%8 != 0  )
        {
            printf("merda\n");
            break;
        }
}
