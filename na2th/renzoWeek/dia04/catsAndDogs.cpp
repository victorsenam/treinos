#include<bits/stdc++.h>
using namespace std;
typedef int num;
typedef int node;
typedef int edge;
const int V = 500;
const int E = 2*V*V;

int adj[V], label[V], in[V], out[V], vs;
int to[E], es;

int main()
{
    int tc;
    scanf("%d", &tc);
    while(tc--)
    {
        scanf("%d %d %d", &c, &d, &vs);
        for(int i=0;i<vs;i++)
        {
            char k1, k2;
            scanf(" %c%d %c%d")
        }
    }
}
