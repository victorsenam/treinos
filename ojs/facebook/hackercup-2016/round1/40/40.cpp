#include <bits/stdc++.h>

using namespace std;

typedef unsigned int num;

const int K = 4;
const int N = (1<<4);
const int M = (1<<N);

int visi[K][N][M];
bool memo[K][N][M];
int t, n, k;

bool pd (int k, int i, num mask) {
    if (
}

int main() {
    scanf("%d", &t);

    for (int ts = 1; ts <= t; ts++) {

    }
}
